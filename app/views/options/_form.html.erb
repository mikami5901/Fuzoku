<%= form_with(model: option) do |form| %>
  <% if option.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
        <% option.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :girl_id, style: "display: block" %>
    <%= form.collection_select(:girl_id, @girls, :id, :name) %>
  </div>

  <div>
    <%= form.label :o_name, style: "display: block" %>
    <%= form.collection_select(:o_name, @option_lists, :option_name, :option_name) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
